$(document).ready(function(){

    // $(selector).accion();

    // $ - Indicamos que vamos a acceder a jQuery.
    // selector - Accedemos al elemento que queremos trabajar.
    // accion - Lo que queremos hacer con el elemento seleccionado.

    // $('h1').hide();
    $('h1').show();

});

//También podemos hacerlo de esta forma:
$(function(){
    //Código que se ejecutara hasta que el documento haya cargado.
});